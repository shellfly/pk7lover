{% extends 'basic.html' %}
{% block content %}
{% load i18n %}
<div class="span9">
  <div class="row">
    <div class="pull-left">
    <h5>所有活动</h5>
    </div>
    <div class="pull-right">
      <a href="{% url 7create_activity %}">创建新活动</a>
      </div>
  </div>

  <div class="7activities">
  	<ul class="unstyled">
  		{% for activity in activities %}
  			<li class="7activity ">
                <div>
                  <p> <a href="{% url 7activity activity.id %}">{{activity.name}}</a>
                  <br /><small>{{activity.photo_num}}人参加</small></p>
                </div>
                <div class="7listphotos">
                  {%load keyvalue%}
                  <ul class="unstyled">
                    {% for photo in photos|keyvalue:photosaying %}
                    <li style="display:inline">
                      <a href="{% url 7single_photo photo.id %}">
                      <img src="{{MEDIA_URL}}{{ photo.thumb64 }}" /></a>
                    </li>
                  {% endfor %}
              </ul>
                </div>
                
  			</li>
  		{% endfor%}
  	</ul>
  </div>
</div>

<div class="span3 pull-right">

</div>
{% endblock content %}
